# Breakout v5 RL Project — full dependency set.
# (루트 ../requirements.txt 과 동일하게 유지한다.)
# 평가용으로 고정된 3개 패키지 (Task Definition §1.1).
gymnasium[atari]==1.3.0
ale-py==0.11.2
autorom[accept-rom-license]==0.6.1

# --- Stable-Baselines3 -------------------------------------------------------
# gymnasium 1.3.0 은 SB3 PyPI 2.8.0 의 캡(<1.3.0) 과 충돌하므로
# 1.3.0 호환 cap 이 풀린 master 를 git 에서 설치한다.
stable_baselines3 @ git+https://github.com/DLR-RM/stable-baselines3.git
sb3_contrib       @ git+https://github.com/Stable-Baselines-Team/stable-baselines3-contrib.git

# --- 그 외 코어 -------------------------------------------------------------
opencv-python==4.13.0.90     # AtariWrapper(WarpFrame) 및 play 창 확대
tensorboard==2.20.0
moviepy==2.2.1

# --- Config / report tooling ------------------------------------------------
PyYAML==6.0.2
pandas==2.2.3
matplotlib==3.10.0
tqdm>=4.66
rich>=13.7

# Optional but recommended for rigorous comparison plots / statistics
# rliable>=1.2.0

# ---------------------------------------------------------------------------
# Usage:
#   conda create -n breakout python=3.11 -y
#   conda activate breakout
#   pip install -r docs/requirements.txt   # 또는 루트 requirements.txt
#   AutoROM --accept-license
#
# PyTorch 는 시스템 CUDA 에 맞춰 별도로 설치하세요. (Manual §5)
